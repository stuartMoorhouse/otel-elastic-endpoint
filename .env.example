# OpenTelemetry Configuration for Elastic Cloud
# Copy this file to .env and fill in your actual values from Elastic Cloud

# OTLP Endpoint - Get this from Elastic Cloud:
# Go to: Add data → Application → OpenTelemetry → Managed OTLP Endpoint
# Format: https://your-deployment-id.ingest.your-region.aws.elastic.cloud:443
OTEL_EXPORTER_OTLP_ENDPOINT=https://your-deployment-id.ingest.your-region.aws.elastic.cloud:443

# Authorization Header with API Key - Get this from Elastic Cloud:
# The value will be in the format: Authorization=ApiKey base64-encoded-key
OTEL_EXPORTER_OTLP_HEADERS=Authorization=ApiKey your-base64-encoded-api-key

# Resource Attributes (optional) - Customize as needed:
# Format: service.name=<app-name>,service.version=<app-version>,deployment.environment=<environment>
OTEL_RESOURCE_ATTRIBUTES=service.name=flask-sha256-hasher,service.version=1.0.0,deployment.environment=production
